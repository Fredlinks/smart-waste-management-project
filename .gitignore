node_modules/
build/
dist/
coverage/
# Ignore the runtime data directory at the repo root, but NOT src/data/
# (which contains the Ghana region geography data shipped with the app).
/data/
.DS_Store
*.log
.env*
!.env.example
